# Your Linear personal API key — create one at https://linear.app/settings/api
LINEAR_API_KEY=lin_api_xxxxxxxxxxxxxxxxxxxx

# Default team key to file issues under (e.g. ENG, INFRA, DESIGN)
# Can be overridden per-snippet with // team: <key> or via the --team CLI flag
LINEAR_TEAM=ENG
